version: "3.9"

services:
  taxonomy_mining:
    build: .
    tty: true
    environment:
      - DISPLAY=${DISPLAY}
    network_mode: host
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix
      - /var/run/docker.sock:/var/run/docker.sock
      - ./mining_data:/root/mining_data

